#include <stdio.h>

int main() {
    int num;
    char ch;
    scanf("%d", &num);
    scanf(" %c", &ch); 
    char intToChar = (char)num;
    int charToAscii = (int)ch;
    printf("Integer of %d is %c\n", num, intToChar);
    printf("Character of %c is %d\n", ch, charToAscii);

    return 0;
}
